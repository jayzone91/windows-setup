@{
    ComputerName = "Jay-PC"

    Taskbar      = @{
        AutoHide = $false
    }

    StartMenu    = @{
        ShowRecentlyAddedApps = $false
        ShowRecentItems       = $false
        ShowRecommendations   = $false
        ShowMostUsedApps      = $true
    }
}
